name: CI

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

env:
  JAVA_VERSION: '17'

jobs:
  build:
    name: Сборка и тесты
    runs-on: ubuntu-latest

    steps:
      - name: Checkout репозитория
        uses: actions/checkout@v4

      - name: Настройка JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'
          cache: maven

      - name: Установка прав на выполнение mvnw
        run: chmod +x ./mvnw

      - name: Кеширование Maven зависимостей
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Сборка проекта
        run: ./mvnw compile -q -DskipSpotlessCheck=true

      - name: Запуск тестов с покрытием
        run: ./mvnw test jacoco:report -q -DskipSpotlessCheck=true

      - name: Извлечение покрытия кода
        id: coverage
        run: |
          # Извлекаем процент покрытия из CSV отчёта
          if [ -f target/site/jacoco/jacoco.csv ]; then
            # Суммируем покрытые и пропущенные инструкции
            COVERED=$(awk -F',' 'NR>1 {sum+=$4} END {print sum}' target/site/jacoco/jacoco.csv)
            MISSED=$(awk -F',' 'NR>1 {sum+=$5} END {print sum}' target/site/jacoco/jacoco.csv)
            TOTAL=$((COVERED + MISSED))
            if [ $TOTAL -gt 0 ]; then
              PERCENT=$((COVERED * 100 / TOTAL))
              echo "coverage=${PERCENT}%" >> $GITHUB_OUTPUT
              echo "## 📊 Отчёт о покрытии кода" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "| Метрика | Значение |" >> $GITHUB_STEP_SUMMARY
              echo "|---------|----------|" >> $GITHUB_STEP_SUMMARY
              echo "| Покрытие инструкций | **${PERCENT}%** |" >> $GITHUB_STEP_SUMMARY
              echo "| Покрытые инструкции | ${COVERED} |" >> $GITHUB_STEP_SUMMARY
              echo "| Пропущенные инструкции | ${MISSED} |" >> $GITHUB_STEP_SUMMARY
            fi
          fi

      - name: Загрузка отчёта JaCoCo
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: target/site/jacoco/
          retention-days: 14

      - name: Проверка минимального покрытия
        run: ./mvnw jacoco:check -q -DskipSpotlessCheck=true
        continue-on-error: true

      - name: Сборка JAR
        run: ./mvnw package -DskipTests -q -DskipSpotlessCheck=true

      - name: Загрузка артефакта сборки
        uses: actions/upload-artifact@v4
        with:
          name: application-jar
          path: target/*.jar
          retention-days: 7

  lint:
    name: Проверка стиля кода
    runs-on: ubuntu-latest

    steps:
      - name: Checkout репозитория
        uses: actions/checkout@v4

      - name: Настройка JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'
          cache: maven

      - name: Установка прав на выполнение mvnw
        run: chmod +x ./mvnw

      - name: Применение форматирования Spotless
        run: ./mvnw spotless:apply -q

      - name: Проверка форматирования (Spotless)
        run: ./mvnw spotless:check -q

      - name: Проверка стиля (Checkstyle)
        run: ./mvnw checkstyle:check -q
        continue-on-error: true

      - name: Проверка изменений после форматирования
        run: |
          if git diff --quiet; then
            echo "✅ Код соответствует стандартам форматирования" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Обнаружены изменения форматирования. Запустите 'mvn spotless:apply' локально." >> $GITHUB_STEP_SUMMARY
            git diff --stat
          fi
